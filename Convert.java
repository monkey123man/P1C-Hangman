/** Covert between String and Characters.
*
* Stewart Chan
*
*/

// Convert a String into an array of char
String word = "hello";
char[] chars = word.toCharArray();

// Convert an array of char into a String
char[] chars = {'h', 'e', 'l', 'l', 'o'};
String str = new String(chars);
